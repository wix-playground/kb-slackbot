{
  "name": "slack-monday-integration",
  "version": "1.0.0",
  "description": "Slack and Monday.com integration app",
  "main": "app.js",
  "scripts": {
    "build": "echo 'No build step needed'",     // or compile TypeScript, etc.
    "lint": "eslint . --max-warnings=0",
    "test": "jest",                              // or a smoke test: node app.js --version
    "ci:smoke": "node app.js --check-health"     // custom health‑check script
  "start": "node app.js"
},
  "dependencies": {
    "@slack/bolt": "^3.14.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
